{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "CE Host",
  "version": "0.1.0",
  "identifier": "com.ce-net.host",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:5180",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../dist"
  },
  "app": {
    "windows": [
      {
        "title": "CE Host — hosting dashboard",
        "width": 1180,
        "height": 760,
        "minWidth": 900,
        "minHeight": 560,
        "resizable": true,
        "label": "main"
      }
    ],
    "security": {
      "csp": "default-src 'self'; connect-src 'self' http://127.0.0.1:8844 http://localhost:8844 ipc: http://ipc.localhost; style-src 'self' 'unsafe-inline'; img-src 'self' data:"
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/icon.png"
    ],
    "category": "Utility",
    "shortDescription": "Host your machine on the CE compute mesh.",
    "longDescription": "CE Host is the qBittorrent-for-compute hosting dashboard: run a CE node, watch what you earn, set resource caps, and walk away — the app supervises the node and keeps earning in the tray."
  }
}
